---
import { getCollection } from 'astro:content';
import Head from '../components/Head.astro';

const posts = (await getCollection('posts'))
  .filter((p) => p.data.type === 'rag-system')
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf());

function readingTime(post) {
  const words = post.body.split(/\s+/).length;
  return Math.max(1, Math.round(words / 200));
}

function fmtDate(d) {
  return d.toLocaleDateString('en-GB', { year: 'numeric', month: 'short', day: '2-digit' });
}
---

<!doctype html>
<html lang="en">
  <head>
    <Head
      title="rag system"
      description="Building RAG systems from scratch — retrieval augmented generation, vector stores, and agentic pipelines."
    />
  </head>
  <body>
    <nav class="navbar">
      <a href="/" class="navbar-path">
        <span class="p-tilde">~/</span><span class="p-name">rahul</span>
      </a>
      <ul class="navbar-nav">
        <li><a href="/">blog</a></li>
        <li><a href="/projects">projects</a></li>
        <li><a href="/llm-architecture">llm-architecture</a></li>
        <li><a href="/rag-system" class="active">rag-system</a></li>
        <li><a href="/everything-else">everything-else</a></li>
        <li><a href="/presentations">presentations</a></li>
        <li><a href="/deck">deck</a></li>
        <li><a href="/about">about</a></li>
      </ul>
    </nav>

    <main class="container">
      <a href="/" class="back-link">← cd ..</a>

      <p class="cmd">
        <span class="dollar">$</span>
        <span class="command"> ls</span>
        <span class="flag"> -lt</span>
        <span class="arg"> ./rag-system/</span>
      </p>

      <ul class="post-list">
        {posts.map((post) => (
          <li class="post-entry">
            <span class="post-date">{fmtDate(post.data.date)}</span>
            <a href={`/posts/${post.slug}`} class="post-link">{post.data.title}</a>
            <span class="post-mins">{readingTime(post)}m</span>
          </li>
        ))}
        {posts.length === 0 && <p class="empty-msg">// no posts yet — check back soon</p>}
      </ul>
    </main>

    <footer class="site-footer"><a href="/privacy">privacy policy</a></footer>
    <script defer src="/_vercel/insights/script.js"></script>
  </body>
</html>
